import {useEffect,useMemo,useState} from 'react';
import {useLocation} from 'react-router-dom';
import {Cell,Pie,PieChart,ResponsiveContainer,Tooltip} from 'recharts';
import {store} from './storage';
type Lead={services?:string[]};
const colors=['#5b36f2','#287cf0','#16a269','#e99a18','#e8547c','#7c8aa2','#14b8a6','#f97316'];
export default function CRMServicesAnalytics(){const location=useLocation();const [leads,setLeads]=useState<Lead[]>(()=>store.get('prospects',[]));useEffect(()=>{const update=()=>setLeads(store.get('prospects',[]));window.addEventListener('roas-change',update);return()=>window.removeEventListener('roas-change',update)},[]);const data=useMemo(()=>{const count=new Map<string,number>();leads.flatMap(lead=>lead.services||[]).forEach(service=>count.set(service,(count.get(service)||0)+1));return Array.from(count,([name,value])=>({name,value})).sort((a,b)=>b.value-a.value)},[leads]);if(location.pathname!=='/crm')return null;return <section className="card crmServicesAnalytics"><div className="servicesChartCopy"><small>INTERESSE COMERCIAL</small><h3>Serviços mais solicitados</h3><p>Distribuição dos serviços de interesse registrados nos leads do pipeline.</p><div className="servicesChartLegend">{data.map((item,index)=><span><i style={{background:colors[index%colors.length]}}/><b>{item.name}</b><em>{item.value} {item.value===1?'lead':'leads'}</em></span>)}</div></div><div className="servicesPie"><ResponsiveContainer width="100%" height={270}><PieChart><Pie data={data.length?data:[{name:'Sem serviços registrados',value:1}]} dataKey="value" nameKey="name" innerRadius={62} outerRadius={100} paddingAngle={3} stroke="none">{(data.length?data:[{name:'',value:1}]).map((_,index)=><Cell fill={data.length?colors[index%colors.length]:'#d9dee7'}/>)}</Pie><Tooltip formatter={(value:any)=>[`${value} lead(s)`,'Solicitações']}/></PieChart></ResponsiveContainer><div className="pieCenter"><strong>{data.reduce((sum,item)=>sum+item.value,0)}</strong><span>solicitações</span></div></div></section>}
