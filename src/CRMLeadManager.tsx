import {useEffect,useState} from 'react';
import {ArrowRight,CheckCircle2,Pencil,Trash2,UserPlus,X} from 'lucide-react';
import {useLocation,useNavigate} from 'react-router-dom';
import {store} from './storage';
import type {AgencyService} from './ServicesManager';
import type {Client,TeamMember} from './types';
import {crmStages as stages,leadServiceIds,serviceEstimate,type CRMLead as Lead,type CRMStage as Stage} from './crm-leads';

const money=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const today=()=>new Date().toISOString().slice(0,10);

export default function CRMLeadManager(){
 const location=useLocation(),navigate=useNavigate();
 const [selected,setSelected]=useState<Lead|null>(null),[converting,setConverting]=useState(false);
 const [selectedServiceIds,setSelectedServiceIds]=useState<string[]>([]),[variableEstimate,setVariableEstimate]=useState(0);
 const [services,setServices]=useState<AgencyService[]>(()=>store.get('services',[]));
 const [team,setTeam]=useState<TeamMember[]>(()=>store.get('team',[]));
 useEffect(()=>{const update=()=>{setServices(store.get('services',[]));setTeam(store.get('team',[]))};window.addEventListener('roas-change',update);return()=>window.removeEventListener('roas-change',update)},[]);
 useEffect(()=>{const click=(event:MouseEvent)=>{if(location.pathname!=='/crm')return;const card=(event.target as HTMLElement).closest<HTMLElement>('.crmPageBoard .leadCard');const leadId=card?.dataset.leadId;if(!leadId)return;const lead=store.get<Lead[]>('prospects',[]).find(item=>item.id===leadId);if(lead){const catalog=store.get<AgencyService[]>('services',[]),ids=leadServiceIds(lead,catalog),fixed=serviceEstimate(ids,catalog).fixedValue;setSelectedServiceIds(ids);setVariableEstimate(Math.max(0,lead.value-fixed));setSelected(lead);setConverting(false)}};document.addEventListener('click',click);return()=>document.removeEventListener('click',click)},[location.pathname]);
 if(!selected)return null;
 const close=()=>{setSelected(null);setConverting(false)};
 const estimate=serviceEstimate(selectedServiceIds,services),estimatedValue=estimate.fixedValue+(estimate.hasVariable||!selectedServiceIds.length?variableEstimate:0);
 const update=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const form=new FormData(e.currentTarget);const all=store.get<Lead[]>('prospects',[]);const edited:Lead={...selected,name:String(form.get('name')).trim(),contact:String(form.get('contact')).trim(),phone:String(form.get('phone')).trim(),responsibleId:String(form.get('responsibleId')),value:estimatedValue,stage:String(form.get('stage')) as Stage,source:String(form.get('source')),nextAction:String(form.get('nextAction')),serviceIds:selectedServiceIds,services:undefined};store.set('prospects',all.map(lead=>lead.id===edited.id?edited:lead));close()};
 const remove=()=>{if(!confirm(`Excluir o lead ${selected.name}? Essa ação não pode ser desfeita.`))return;store.set('prospects',store.get<Lead[]>('prospects',[]).filter(lead=>lead.id!==selected.id));close()};
 const convert=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const clients=store.get<Client[]>('clients',[]);const existing=clients.find(client=>client.sourceLeadId===selected.id||client.id===selected.convertedClientId);if(existing){alert('Este lead já foi convertido em cliente.');close();navigate('/clients');return}const form=new FormData(e.currentTarget),now=new Date().toISOString();const client:Client={id:crypto.randomUUID(),sourceLeadId:selected.id,companyName:String(form.get('companyName')),contactName:String(form.get('contactName')),email:String(form.get('email')),phone:String(form.get('phone')),instagram:String(form.get('instagram')),segment:String(form.get('segment')),city:String(form.get('city')),status:'active',managerId:String(form.get('managerId')),monthlyRevenue:Number(form.get('monthlyRevenue')||0),serviceIds:form.getAll('serviceIds').map(String),startDate:String(form.get('startDate')),notes:String(form.get('notes')),color:selected.color||'#5b36f2',createdAt:now,updatedAt:now};store.set('clients',[client,...clients]);store.set('prospects',store.get<Lead[]>('prospects',[]).map(lead=>lead.id===selected.id?{...lead,stage:'Negócio fechado',nextAction:'Cliente convertido',convertedClientId:client.id}:lead));close();navigate('/clients')};
 if(converting)return <ConversionModal lead={selected} services={services} team={team} onClose={()=>setConverting(false)} onSubmit={convert}/>;
 const converted=Boolean(selected.convertedClientId||store.get<Client[]>('clients',[]).some(client=>client.sourceLeadId===selected.id));
 return <div className="overlay"><div className="modal leadEditModal"><div className="modalHead"><div><small>GERENCIAR LEAD</small><h2>Editar oportunidade</h2><p>{selected.name} · {money(estimatedValue)}</p></div><button className="iconBtn" onClick={close}><X/></button></div>{converted&&<div className="convertedNotice"><CheckCircle2/><div><b>Lead convertido em cliente</b><span>Este vínculo já está registrado no sistema.</span></div></div>}<form className="form" onSubmit={update}><label>Empresa<input name="name" required defaultValue={selected.name}/></label><label>Contato<input name="contact" required defaultValue={selected.contact}/></label><label>Telefone<input name="phone" type="tel" defaultValue={selected.phone} placeholder="(00) 00000-0000"/></label><label>Responsável pelo lead<select name="responsibleId" defaultValue={selected.responsibleId||''}><option value="">Definir depois</option>{team.filter(member=>member.status==='active'||member.id===selected.responsibleId).map(member=><option key={member.id} value={member.id}>{member.name}</option>)}</select></label><label>Etapa<select name="stage" defaultValue={selected.stage}>{stages.map(stage=><option key={stage}>{stage}</option>)}</select></label><label>Origem<select name="source" defaultValue={selected.source}><option>Instagram</option><option>Site</option><option>Indicação</option><option>Google</option><option>WhatsApp</option><option>Outro</option></select></label><label className="full">Próxima ação<input name="nextAction" defaultValue={selected.nextAction}/></label><div className="full"><b className="formGroupLabel">Serviços solicitados</b><small className="formHelper">Os serviços mensais atualizam o valor automaticamente.</small><div className="selectionGrid crmServicesSelect">{services.filter(service=>service.active||selectedServiceIds.includes(service.id)).map(service=><label key={service.id}><input type="checkbox" checked={selectedServiceIds.includes(service.id)} onChange={event=>setSelectedServiceIds(current=>event.target.checked?[...current,service.id]:current.filter(id=>id!==service.id))}/><span><b>{service.name}</b><small>{service.pricingType==='variable'||service.price<=0?'Valor a definir':`${money(service.price)}/mês`}</small></span></label>)}</div></div><div className="leadEstimateBox full"><div><span>Valor automático dos serviços</span><strong>{money(estimate.fixedValue)}</strong></div>{(estimate.hasVariable||!selectedServiceIds.length)&&<label>{selectedServiceIds.length?'Estimativa dos serviços sem valor fixo':'Valor estimado'}<input type="number" min="0" step="0.01" value={variableEstimate||''} onChange={event=>setVariableEstimate(Number(event.target.value)||0)} placeholder="0,00"/></label>}<div className="leadEstimateTotal"><span>Valor estimado total</span><strong>{money(estimatedValue)}</strong></div></div><div className="leadConvertBar full"><div><b>{converted?'Cliente já criado':'O negócio avançou?'}</b><span>{converted?'Abra a página Clientes para gerenciar o cadastro.':'Converta o lead sem digitar novamente os dados comerciais.'}</span></div><button type="button" className="btn convertLead" disabled={converted} onClick={()=>setConverting(true)}>{converted?<CheckCircle2/>:<UserPlus/>}{converted?'Cliente criado':'Converter em cliente'}{!converted&&<ArrowRight/>}</button></div><div className="leadEditActions full"><button type="button" className="btn deleteLead" onClick={remove}><Trash2/> Excluir lead</button><span/><button type="button" className="btn secondary" onClick={close}>Cancelar</button><button className="btn"><Pencil/> Salvar alterações</button></div></form></div></div>
}

function ConversionModal({lead,services,team,onClose,onSubmit}:{lead:Lead;services:AgencyService[];team:TeamMember[];onClose:()=>void;onSubmit:(e:React.FormEvent<HTMLFormElement>)=>void}){const selectedIds=leadServiceIds(lead,services);return <div className="overlay"><div className="modal conversionModal"><div className="modalHead"><div><small>CRM → CLIENTE</small><h2>Converter lead em cliente</h2><p>Revise as informações antes de criar o cadastro.</p></div><button className="iconBtn" onClick={onClose}><X/></button></div><div className="conversionSummary"><span style={{background:lead.color}}>{lead.name.split(' ').map(part=>part[0]).join('').slice(0,2)}</span><div><b>{lead.name}</b><small>{lead.contact} · {lead.source}</small></div><strong>{money(lead.value)}</strong></div><form className="form conversionForm" onSubmit={onSubmit}><label>Empresa<input name="companyName" required autoFocus defaultValue={lead.name}/></label><label>Contato principal<input name="contactName" required defaultValue={lead.contact}/></label><label>E-mail<input name="email" type="email" required placeholder="contato@empresa.com.br"/></label><label>Telefone<input name="phone" required defaultValue={lead.phone} placeholder="(00) 00000-0000"/></label><label>Instagram<input name="instagram" placeholder="@empresa"/></label><label>Segmento<input name="segment" placeholder="Ex.: Saúde e estética"/></label><label>Cidade<input name="city" placeholder="Cidade / Estado"/></label><label>Início do contrato<input name="startDate" type="date" required defaultValue={today()}/></label><label>Receita mensal<input name="monthlyRevenue" type="number" min="0" required defaultValue={lead.value}/><small>Valor recorrente esperado deste cliente.</small></label><label>Gestor responsável<select name="managerId" defaultValue={lead.responsibleId||''}><option value="">Definir depois</option>{team.filter(member=>member.status==='active'||member.id===lead.responsibleId).map(member=><option key={member.id} value={member.id}>{member.name}</option>)}</select></label><div className="full clientServicesField"><b>Serviços contratados</b><small>Os serviços solicitados pelo lead já estão selecionados.</small><div className="clientServicesGrid">{services.filter(service=>service.active||selectedIds.includes(service.id)).map(service=><label key={service.id}><input name="serviceIds" type="checkbox" value={service.id} defaultChecked={selectedIds.includes(service.id)}/><span><b>{service.name}</b><small>{service.pricingType==='variable'||service.price<=0?'Valor variável':`${money(service.price)}/mês`}</small></span></label>)}</div></div><label className="full">Observações<textarea name="notes" defaultValue={`Convertido do CRM. Origem do lead: ${lead.source}.`}/></label><div className="conversionInfo full"><CheckCircle2/><span>Ao concluir, o lead será movido para <b>Negócio fechado</b> e vinculado ao novo cliente.</span></div><div className="formActions full"><button type="button" className="btn secondary" onClick={onClose}>Voltar</button><button className="btn"><UserPlus/> Criar cliente</button></div></form></div></div>}
