{
  "name": "roas-agency",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently -n API,WEB -c magenta,cyan \"npm run dev -w @roas/api\" \"npm run dev -w @roas/web\"",
    "dev:api": "npm run dev -w @roas/api",
    "dev:web": "npm run dev -w @roas/web",
    "build": "npm run build -w @roas/api && npm run build -w @roas/web",
    "build:vercel": "npm run build:vercel -w @roas/web",
    "typecheck:vercel": "tsc -p tsconfig.vercel.json",
    "test": "npm run test -w @roas/api && node --import tsx --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
