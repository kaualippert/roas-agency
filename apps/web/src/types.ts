export type Status='active'|'inactive'|'prospect'|'planning'|'paused'|'completed'|'cancelled'|'todo'|'pending'|'in_progress'|'overdue';
export interface Base {id:string;createdAt:string;updatedAt:string}
export interface Client extends Base {sourceLeadId?:string;companyName:string;contactName:string;email:string;phone:string;instagram:string;segment:string;city:string;cnpj?:string;paymentDay?:number;status:'active'|'inactive'|'prospect';managerId:string;responsibleIds?:string[];monthlyRevenue:number;serviceIds?:string[];services?:string[];startDate:string;notes:string;color:string}
export interface Project extends Base {clientId:string;name:string;description:string;status:'planning'|'active'|'paused'|'completed'|'cancelled';priority:string;responsibleId:string;responsibleIds?:string[];dueDate:string;progress:number;progressMode?:'manual'|'tasks';pricingType?:'monthly'|'variable';monthlyValue:number;variableValue?:number;serviceIds?:string[];services?:string[];channels:string[]}
export interface Task extends Base {title:string;description:string;clientId:string;projectId:string;responsibleId:string;responsibleIds?:string[];status:'todo'|'pending'|'in_progress'|'completed'|'overdue';priority:'low'|'medium'|'high'|'urgent';dueDate:string;tags:string[];position:number;commentsCount:number;attachmentsCount:number}
export type AccessArea='general'|'marketing'|'finance'|'settings';
export interface TeamMember extends Base {name:string;email:string;role:string;roles?:string[];clientIds?:string[];accessAreas?:AccessArea[];department:string;status:string;color:string}
export interface GenericItem extends Base {name:string;clientId?:string;status:string;value?:number;date?:string;description?:string;category?:string;fileId?:string;mimeType?:string;size?:number}
